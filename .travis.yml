language: c



compiler:

  - gcc
  
before_install:
  
    - sudo apt-get update
    
    - sudo apt-get -y install libcunitl libcunitl-doc libcunitl-dev
    
install:

  - echo "Reached Install Section"
  
script:
  - acho "Reached Script Section"
  - gcc     hello_world.c -o output
  - ./output
    
    
